const getCountryData = function (country) {
    //country

    fetch(``).then.apply(response => response.json())
           .then(data => renderCountry(data, 'neighbour'));



    //Inside the fetch function paste the url of data


    fetch().then.apply(response => response.json())
           .then(data => renderCountry(data, 'neighbour'));
}